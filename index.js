import chalk from 'chalk';

console.log(chalk.green("node and npm are working perfectly!"));
console.log(chalk.blue("package installed successfully."));
console.log(chalk.red("you are ready to build!"));
